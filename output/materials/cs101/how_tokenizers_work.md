# How Tokenizers Work: Mechanics and Parameters

A tokenizer is fundamentally a function that maps a text string to a list of integers. It's the engine that performs the conversion from human language to machine-readable numerical sequences.

## The Tokenizer Function

Conceptually, a tokenizer takes an input string and outputs a list of numerical IDs:

`